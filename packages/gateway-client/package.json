{
  "name": "@volt/gateway-client",
  "version": "0.1.0",
  "private": true,
  "description": "Directorio de conexiones cargador → pod (Redis) y cliente de la API interna del gateway OCPP",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "ioredis": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
